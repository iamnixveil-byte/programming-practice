# Vosk library is used in python for speech-to-text
# To install the library use <pip install vosk> in your terminal
# Furthermore you will need to download a pre-trained language model(eg.english) 
# From the website unzip it into a local directory 
 